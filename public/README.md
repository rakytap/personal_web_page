# Public Folder

Place your profile photo here as `profile-photo.jpg` (or update the path in `app/components/ProfileCard.tsx` to match your filename).

Supported formats: `.jpg`, `.jpeg`, `.png`, `.webp`

The image will be displayed in a circular frame on the home page.

